{
  "name": "knowledge-base",
  "version": "1.0.0",
  "description": "Knowledge Base visualization with D3.js",
  "main": "index.html",
  "scripts": {
    "start": "live-server --port=8000 --no-browser"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
